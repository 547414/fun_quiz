---
description: Core clean code standards for this repository.
alwaysApply: true
---

# Core Clean Code

- 单一职责：函数/组件只做一件事；职责变多时立即拆分。
- 命名可读：名称表达业务意图，避免 `data`、`tmp`、`handleIt` 等模糊命名。
- 控制复杂度：优先早返回，避免超过 3 层嵌套，避免超长函数。
- 边界清晰：I/O、网络、数据库、副作用逻辑与纯计算逻辑分离。
- 错误处理：禁止吞异常；报错信息需要包含可定位上下文。
- 删除重复：重复逻辑抽取复用，禁止复制粘贴后“微改”。
- 最小改动：优先局部改动，保持与现有目录和分层一致。
- 自检要求：提交前检查类型、空值分支、边界条件和回归风险。

## Quick Checks

- 这个改动是否降低了可读性？
- 新增抽象是否真的减少复杂度？
- 是否引入了隐式耦合或隐藏副作用？
